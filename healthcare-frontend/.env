import axios from 'axios';

const api = axios.create({
  // Vite will automatically pick the right URL based on the environment
  baseURL: import.meta.env.VITE_API_URL, 
  headers: {
    'Content-Type': 'application/json'
  }
});

export default api;
